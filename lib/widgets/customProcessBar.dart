import 'package:flutter/material.dart';

class Customprocessbar extends StatelessWidget {
  const Customprocessbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
